name: CI

on:
  push:
    branches: [main]
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - '.golangci.yml'
      - 'Makefile'
      - '.github/workflows/**'
      - '!**.md'
  pull_request:
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - '.golangci.yml'
      - 'Makefile'
      - '.github/workflows/**'
      - '!**.md'

jobs:
  ci:
    permissions:
      contents: read
      pull-requests: read
    uses: bnema/gh-actions/.github/workflows/go-ci.yml@main
    with:
      go-version: '1.26.2'
      lint-version: latest
      lint-timeout: 5m
      test-flags: '-race -v ./...'
